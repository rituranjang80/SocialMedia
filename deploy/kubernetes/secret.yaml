apiVersion: v1
kind: Secret
metadata:
  name: brightbean-studio-secrets
type: Opaque
stringData:
  # Replace before production deploy (scripts/start-k8s.* auto-generate these).
  SECRET_KEY: change-me-to-a-random-string
  ENCRYPTION_KEY_SALT: change-me-to-a-random-string
